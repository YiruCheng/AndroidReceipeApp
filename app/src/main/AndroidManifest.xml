<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.semanticweb.receipe.receipeapp" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectIngredientActivity"
            android:label="@string/title_activity_select_ingredient"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShowRecommendationActivity" >
        </activity>
        <activity
            android:name=".TranslateActivity"
            android:label="@string/title_activity_translate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IngredientFragment"
            android:label="@string/title_activity_ingredient_fragment"
            android:parentActivityName="android.support.v4.app.FragmentActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.support.v4.app.FragmentActivity" />
        </activity>
        <activity
            android:name=".PagerActivity"
            android:label="@string/title_activity_pager" >
        </activity>
        <activity
            android:name=".RecipeDetailActivity"
            android:label="@string/title_activity_recipe_detail" >
        </activity>
    </application>

</manifest>